# probabilistic-dongulator
The Probabilistic Dongulator is a probabilistic random number generator that generates a user-defined number of random numbers based on a variety of different statistical distributions.
